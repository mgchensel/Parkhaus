import styled from "styled-components";

const SEntry = styled.button`
  border: 1px solid black;
  background-color: rgba(0, 0, 0, 0.4);
  text-align: left;
  width: 100%;
`;

export default SEntry;
